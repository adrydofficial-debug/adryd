import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const BoardLocation = () => {
  return (
    <View>
      <Text>BoardLocation</Text>
    </View>
  );
};

export default BoardLocation;

const styles = StyleSheet.create({});
